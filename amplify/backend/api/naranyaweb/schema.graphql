type Lead 
  @model 
  @auth(rules: [{ allow: public, operations: [create] }]) {
   id: ID!
   name: String!
   email: AWSEmail!,
   interes:String,
   mensaje:String,
   recaptchaToken: String,
   recaptchaScore: Float
}
